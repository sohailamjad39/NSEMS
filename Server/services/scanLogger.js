/**
 * NSEMS/Server/services/scanLogger.js
 * 
 * Minimal scan logger (for future CRDT sync)
 */
export const logScan = async (data) => {
  // Placeholder - will implement CRDT sync later
  console.log('SCAN LOG:', data);
};